# medicaldatavalidatorfcc
In the freeCodeCamp Python Certification, I encountered one of the most longest workshops, it takes my recent knowledge of all of the basic data types such as lists and dictionaries and makes me apply into this project scenario. Its going to make me take a couple of days and a long explanation on what I learned.

#what I learned
Day 1:
I learned how to create a list, then put a dictionary inside and assign keys to values. Additionally, I learned how you can enumerate items in a dictionary, which made it easier for my for and if statements. For example the function validate(data), there is a variable 'is_sequence' that checks the data whether if its a list or a tuple to avoid an error such as entering a string only or a single data type only. Then the 'key_set' checks whether if the set contains these key things in order for each patient record must have.

Day 2:
I learned where you can import stuff such as re for this instance, and then found out about the functions such as re.IGNORECASE which made it easier when the function constraints checks for requirements in the medical records. Furthermore, I learned about the quantifiers. For example the p\d means it follows one digit after the letter 'p'. There are multiple variations but the one that I went through was the p\d+, which is basically one or more after the letter 'p'.
